#Include <iostream>
#Include <math.h>

void main()
{
  // Declare the variables
  double mapRow, mapColumns;
  vector <int> vectorMap;
  
  // Ask the user for the input
  cout << "Enter the number of rows in the map" << endl;
  cin >> mapRow;
  cout << "Enter the number of col in the map" << endl;
  cin >> mapColumns;
}
